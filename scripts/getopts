#!/bin/bash

# pass arguments from cli
while getopts a:c:s:p:h: option
do
    case "${option}"
        in
        a) atac=${OPTARG};;
        c) cite=${OPTARG};;
        s) samples=${OPTARG};;
        p) params=${OPTARG};;
        h) harmonize=${OPTARG};;
    esac
done

